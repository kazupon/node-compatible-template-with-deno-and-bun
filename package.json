{
  "name": "node-complatible-with-deno-and-bun",
  "license": "MIT",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "git config --local core.hooksPath .githooks",
    "preinstall": "bun scripts/preinstall.js",
    "lint": "deno lint",
    "format": "deno fmt",
    "build": "bun run build:bar && bun run build:foo",
    "build:bar": "cd packages/bar && bun run build",
    "build:foo": "cd packages/foo && bun run build",
    "dev:foo": "cd packages/foo && bun run dev",
    "test": "bun run test:bar && bun run test:foo",
    "test:bar": "cd packages/bar && bun run test",
    "test:foo": "cd packages/foo && bun run test"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,md,json,jsonc}": [
      "deno fmt"
    ],
    "*.{js,ts,jsx,tsx}": [
      "deno lint"
    ]
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.6.0",
    "lint-staged": "^14.0.0",
    "typescript": "^5.2.2",
    "unbuild": "^2.0.0"
  }
}
